# === Source Code (not needed in image) ===
src/
pom.xml
*.md
HELP.md

# === Build Tools (not needed in image) ===
.mvn/
mvnw
mvnw.cmd
target/classes/
target/test-classes/
target/maven-archiver/
target/maven-status/
target/generated-sources/
target/generated-test-sources/
target/surefire-reports/
target/failsafe-reports/

# === Keep only the final JAR ===
# We want target/*.jar, so don't ignore target/ entirely
# Instead, ignore specific target subdirectories above

# === IDE Files ===
.idea/
*.iml
*.iws
*.ipr
.vscode/
.classpath
.project
.settings/
.factorypath
.apt_generated/
.sts4-cache/

# === OS Files ===
.DS_Store
Thumbs.db
*.tmp
*.swp
*~

# === Git ===
.git/
.gitignore

# === Docker ===
Dockerfile*
docker-compose*.yml
.dockerignore

# === Environment ===
.env
.env.*
*.env

# === Logs ===
*.log
logs/

# === Testing ===
test-output/
.pytest_cache/

# === Node.js (if you have frontend assets) ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Other Build Systems ===
build/
.gradle/
gradle/
gradlew
gradlew.bat